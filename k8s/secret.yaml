apiVersion: v1
kind: Secret
metadata:
  name: anthropic-credentials
  namespace: kagent
type: Opaque
stringData:
  # Replace with your real Anthropic API key before applying
  ANTHROPIC_API_KEY: "<replace-with-real-key>"
  ANTHROPIC_BASE_URL: "<replace-with-base-url-if-applicable>"
